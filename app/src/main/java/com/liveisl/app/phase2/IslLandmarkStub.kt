package com.liveisl.app.phase2

/**
 * Phase 2 placeholder: MediaPipe Hands/Pose → LiteRT → TTS.
 * Native MediaPipe/CameraX deps are intentionally not linked in Phase 1 so the
 * APK stays clean on 16 KB page-size emulators/devices.
 */
class IslLandmarkStub {
    fun describe(): String {
        return "Phase 2 reserved: add MediaPipe Tasks Vision + LiteRT when training starts."
    }

    fun close() = Unit
}
